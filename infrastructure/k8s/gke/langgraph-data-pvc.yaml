# infrastructure/k8s/gke/langgraph-data-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: langgraph-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 1Gi
